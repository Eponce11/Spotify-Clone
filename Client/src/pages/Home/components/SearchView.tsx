import { MainViewContainer, SearchTopbar } from "./";

const SearchView = () => {
  return (
    <MainViewContainer>
      <SearchTopbar />
    </MainViewContainer>
  );
};

export default SearchView;
