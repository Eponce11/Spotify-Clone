import { MainViewContainer, SearchTopResults, SearchTopbar } from "./";

const SearchView = () => {
  return (
    <MainViewContainer>
      <SearchTopbar />
      <SearchTopResults />
    </MainViewContainer>
  );
};

export default SearchView;
